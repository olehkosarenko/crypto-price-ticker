<?xml version="1.0"?>
<ruleset name="CryptoPriceTicker">
    <description>PHPCS for WordPress Coding Standards.</description>
    <file>./crypto-price-ticker.php</file>
    <file>./src</file>

    <rule ref="WordPress"/>
    <rule ref="WordPress-Docs"/>
    <rule ref="WordPress-Core"/>
    <rule ref="WordPress-Extra"/>

    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>assets/*</exclude-pattern>
    <exclude-pattern>build/*</exclude-pattern>

    <rule ref="Generic.Files.LowercasedFilename">
        <exclude-pattern>src/.*</exclude-pattern>
    </rule>
    <rule ref="WordPress.Files.FileName">
        <exclude-pattern>src/.*</exclude-pattern>
    </rule>

    <!-- Allow slashes in hook names (used for Interactivity API and block namespaces) -->
    <rule ref="WordPress.NamingConventions.ValidHookName.UseUnderscores">
        <exclude-pattern>src/.*</exclude-pattern>
    </rule>

    <config name="testVersion" value="8.0-"/>
    <arg name="colors"/>
    <arg value="p"/>
</ruleset>
